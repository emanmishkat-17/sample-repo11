# Contact Management Web App

Simple static web app to add, edit, delete, and list contacts.

How to use:
- Open `index.html` in a browser (no build step required).
- Add contacts using the **Add Contact** button.
- Edit or delete contacts using the actions in the table.
- Contacts persist in `localStorage` in your browser.

Technologies: HTML, CSS, Bootstrap, JavaScript
